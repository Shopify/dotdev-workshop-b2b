# Internal SE planning + build notes - not for the public workshop repo
_internal/

# OS / editor
.DS_Store

# Node / Shopify CLI (app scaffold adds its own too)
node_modules/
.shopify/
*.log
# Per-attendee linked config the CLI writes on first `dev` (keep base shopify.app.toml tracked)
shopify.app.*.toml

# Python (setup fallback script cache)
__pycache__/
*.pyc

# Build / codegen output (regenerated by shopify app dev / function build)
dist/
generated/
