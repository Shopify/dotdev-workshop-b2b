query CartPaymentMethodsTransformRunInput {
  # STARTER STUB. TODO (prompt 05): add the purchasing company id and, on each line's
  # ProductVariant, product { metafield(namespace: "$app", key: "b2b-prebooking") { value } }
  # so the function can detect a pre-book cart.
  cart {
    lines {
      merchandise {
        __typename
      }
    }
  }
  paymentMethods {
    id
    name
  }
}
